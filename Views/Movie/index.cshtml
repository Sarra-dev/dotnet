@model IEnumerable<MyFirstApp.Models.Movie>

<!--<h4>
@foreach(var item in Model)
{
    <h2>@item.name</h2>
}
</h4>

<h2>🎬 Movies List</h2> -->

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.name</td>
            </tr>
        }
    </tbody>
</table>


